{
  "extends": ["//"],
  "tasks": {
    "dev": { "passThroughEnv": ["PORT", "API_URL"] },
    "start": { "passThroughEnv": ["PORT", "API_URL"] },
    "build": {
      "outputs": [".next/**", "!.next/cache/**", "!.next/dev/**"]
    },
    "typecheck": {
      "outputs": [".next/types/**", "*.tsbuildinfo"]
    }
  }
}
