{
  "extends": ["//"],
  "tasks": {
    "dev": {
      "passThroughEnv": ["PORT", "API_URL", "WEB_URL", "NEXT_PUBLIC_PRIVY_APP_ID"]
    },
    "start": {
      "passThroughEnv": ["PORT", "API_URL", "WEB_URL", "NEXT_PUBLIC_PRIVY_APP_ID"]
    },
    "build": {
      "outputs": [".next/**", "!.next/cache/**", "!.next/dev/**"],
      "inputs": ["$TURBO_DEFAULT$", ".env", ".env.*"],
      "env": ["API_URL", "WEB_URL", "NEXT_PUBLIC_PRIVY_APP_ID"]
    },
    "typecheck": {
      "outputs": [".next/types/**", "*.tsbuildinfo"]
    }
  }
}
